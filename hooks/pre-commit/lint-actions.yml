min_version: 2.1.0

pre-commit:
  parallel: true
  jobs:
    - name: lint-actions
      glob: .github/workflows/*.yml
      tags: [lint, github]
      run: actionlint {staged_files}
      timeout: 20s
