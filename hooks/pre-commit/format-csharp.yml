min_version: 2.1.0

pre-commit:
  parallel: true
  jobs:
    - name: format-csharp
      glob: "**/*.cs"
      run: dotnet format --severity warn --verbosity normal --verify-no-changes --include {staged_files}
      timeout: 30s
