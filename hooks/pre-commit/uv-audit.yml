# https://lefthook.dev/configuration/glob_matcher/
glob_matcher: doublestar

min_version: 2.1.0

pre-commit:
  parallel: true
  jobs:
    - name: uv-audit
      tags: [python]
      glob:
        - "**/uv.toml"
        - "**/uv.lock"
        - "**/pyproject.toml"
      run: uv audit
