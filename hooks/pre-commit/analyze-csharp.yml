min_version: 2.1.0

pre-commit:
  parallel: true
  jobs:
    - name: analyze-dotnet
      glob: "**/*.{cs,csproj,sln,slnx}"
      run: dotnet format analyzers --severity warn --verbosity normal --verify-no-changes
      timeout: 20s
