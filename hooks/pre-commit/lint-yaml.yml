min_version: 2.1.0

pre-commit:
  parallel: true
  jobs:
    - name: lint-yaml
      tags: [lint, yaml]
      glob: "*.{yml,yaml}"
      args: -f colored
      run: yamllint {staged_files}
      timeout: 20s
