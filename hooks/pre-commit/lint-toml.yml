pre-commit:
  parallel: true
  jobs:
    - name: check-toml
      glob: "*.toml"
      group:
        piped: true
        jobs:
          - name: format-toml
            run: taplo fmt {staged_files}
            stage_fixed: true
          - name: lint-toml
            run: taplo lint {staged_files}
